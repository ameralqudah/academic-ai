# Render Blueprint — https://render.com/docs/blueprint-spec
#
# Creates the web service with the correct build and start commands. The database
# lives on Neon (set DATABASE_URL below), so no Postgres is declared here.
#
# The build runs migrations and seeds the subscription plans before compiling, so
# a fresh deploy comes up with a working schema and a default plan.
services:
  - type: web
    name: academic-ai
    runtime: node
    plan: starter
    region: frankfurt
    branch: main
    buildCommand: npm ci && npm run db:migrate && npm run db:seed && npm run build
    startCommand: npm run start
    healthCheckPath: /api/health
    autoDeploy: true
    envVars:
      - key: NODE_VERSION
        value: "22"
      - key: NODE_ENV
        value: production
      # --- set these in the Render dashboard (marked sync:false = not in git) ---
      - key: DATABASE_URL
        sync: false
      - key: AUTH_SECRET
        generateValue: true
      - key: ANTHROPIC_API_KEY
        sync: false
      - key: RESEND_API_KEY
        sync: false
      - key: EMAIL_FROM
        sync: false
      - key: AUTH_URL
        sync: false
      - key: APP_URL
        sync: false
      - key: SEED_ADMIN_EMAIL
        sync: false
      - key: SEED_ADMIN_PASSWORD
        sync: false
      # --- safe defaults ---
      - key: AI_PROVIDER
        value: anthropic
      - key: EMAIL_PROVIDER
        value: resend
      - key: BILLING_PROVIDER
        value: manual
      - key: MANUAL_BILLING_ALLOW_PAID
        value: "false"
      - key: RATE_LIMIT_STORE
        value: memory
      - key: AUTH_RATE_LIMIT_MAX
        value: "5"
      - key: LOG_LEVEL
        value: info
